# Готовность к production: разбор пробелов и дорожная карта

> Честная позиция: текущий продукт — **инвестируемый прототип** (ультра-MVP, сессии 00–16 и частичное release-hardening), не production-релиз. Спека (SOUL_ULTRA_MVP_CONTEXT.md) прямо запрещает продакшен-запуск и биллинг до прохождения kill criteria, внешней проверки P0 и явного решения основателя (§2.3, строка 191; SESSION-15, строка 1909). Этот документ — карта того, что отделяет прототип от production, по слоям, с гейтами и приоритетами.

## 1. Что уже на уровне продакшена (не нужно переделывать)

- **Локальные данные**: SQLCipher (шифрование на диске), атомарный импорт/экспорт с подписью ed25519, капы KDF и лимиты размера; на Windows приватный device key и локальная MCP capability защищены DPAPI с миграцией legacy-файлов.
- **Целостность**: capability и квитанции подписаны устройством, проверка подписи fail-closed, атомарное выполнение, повторная оценка политики, защита сохранённой нагрузки (SESSION-12 ultra-review).
- **Изоляция**: проверка владельца записи (soul_id) для сущностей/оценок, trusted-sender в браузерном relay, query_only для bridge/MCP, лимиты IPC, строгий CSP, отказ от shell-плагина (SESSION-12, SESSION-13).
- **Инженерия**: 245 Rust debug-проверок + 5 release-only и 270 TS-тестов, clippy/fmt/typecheck/lint чисто, детерминированные property-тесты политик и gateway.
- **Release checks**: `pnpm release:check` поднимает MSVC Build Tools, переопределяет несовместимый глобальный LLVM-MinGW linker, собирает реальные `soul-mcp`/`soul-bridge` sidecars, проверяет их SHA-256 и запускает release E2E/benchmark tests на Windows.

## 2. Пробелы по слоям

### 2.1 Продукт и ядро (P0-качество → релизное)

| Пробел | Что нужно | Откуда |
|---|---|---|
| Production performance gate | Закрыто для целевого P0: release gate 6 августа 2026 года показал p95 политики 194.6 µs; cold context на 1 000 сущностей 48.7877 ms; cached context на 10 000 сущностей 49 µs. Cold 10k остаётся regression path, не интерактивным SLO | SESSION-14, SESSION-17 |
| Расход токенов модели | Частично закрыто: кеш извлечения по content hash, кеш контекста по версии состояния+политики, input-token и estimated-cost квитанции. Реальных model output tokens нет: SOUL сам модель не вызывает | SESSION-14 |
| UX без полировки | Частично закрыто: честная загрузка/ошибки с ID корреляции, клавиатурная tab-навигация, узкие окна и privacy disclosure перед restore. Нужен ручной a11y-аудит | SESSION-16 |
| Демонстрация требует основателя | Частично закрыто: 55-секундный сценарий и read-only synthetic demo с явными метками. Playwright flow остаётся незакрытым: registry npm был недоступен при попытке добавить runner | SESSION-16 |
| Нет бенчмарков в репозитории | Закоммиченный регрессионный перф-тест | SESSION-14 (приёмка) |

### 2.2 Безопасность (настоящий Gateway и ключи)

| Пробел | Что нужно | Откуда |
|---|---|---|
| Gateway — имитация | Реальная изоляция учётных данных: настоящие коннекторы, криденшелы вне процесса, подписанные запросы к провайдерам, верификация ответов по каналу, replay protection на стороне провайдера | §4.11 (строка 646), строка 1111 |
| Ключи устройства файлами | Windows закрыт частично: приватный ключ и capability защищены DPAPI текущего пользователя. Для macOS/Linux всё ещё нужны Keychain/Secret Service и ротация ключей | SESSION-03, partial release hardening |
| Корень доверия — самоподпись | Первый ключ — корень доверия без внешней привязки; нужен механизм привязки к аккаунту/устройству (P1) | SESSION-03 |
| Fuzzing только детерминированный | Случайные/распределённые сиды property-тестов, seed-инъекция для CI-бассейна | SESSION-13 (риск 2) |
| Нет телеметрии безопасности | Отчёты о сбоях и диагностика — только по согласию, с маскировкой личного содержимого | SESSION-17 |
| Экспорт/импорт без инструкций | Документированные процедуры резервного копирования и восстановления | SESSION-17 |

### 2.3 Распространение и инфраструктура

| Пробел | Что нужно | Откуда |
|---|---|---|
| Сборки не подписаны | Подписанные сборки (Windows Authenticode / macOS Developer ID + notarization), метаданные выпуска | SESSION-17 |
| Нет безопасного обновления | Канал обновлений с проверкой подписи, путь отката, тест «чистая установка → обновление» | SESSION-17 |
| Нет SBOM и проверки лицензий | SBOM зависимостей, ревизия лицензий перед публикацией; `cargo audit` также требует закрытия или письменного risk acceptance для transitive GTK/glib advisory | SESSION-17 |
| CI только Windows | GitHub Actions запускает frontend/Rust/release gates с pinned Node/pnpm/Rust. Нужны macOS/Linux jobs и подписанный release workflow | Partial release hardening |
| Installer smoke | Закрыто для Windows x64: 6 августа 2026 года `pnpm release:check -- -SkipBuild -Target x86_64-pc-windows-msvc` собрал NSIS installer, сверил SHA-256 app/sidecar payload и выполнил silent clean-install smoke с проверкой `SOUL.exe`, `soul-mcp*.exe` и `soul-bridge*.exe`. Нужны platform-native smoke tests для macOS/Linux | SESSION-17 |
| Нет окружений | staging/production для серверных компонентов | P1-список (строка 862) |

### 2.4 Операции, поддержка, юридическое

| Пробел | Что нужно | Откуда |
|---|---|---|
| Нет политики приватности | Политика приватности + объяснение обработки данных (что локально, что уходит на сервер, что в модель) | SESSION-17 |
| Нет диагностики поддержки | Маскируемая диагностика (без личных данных), ID корреляции в ошибках | SESSION-16, SESSION-17 |
| Нет процесса инцидентов | Реакция на уязвимости, контактный канал, embargo-политика | Дополнение |
| Нет чек-листа выпуска | Зафиксированный release checklist с приёмкой SESSION-17 | SESSION-17 |
| GDPR/региональные требования | Экспорт и удаление данных (уже в P0 — «экспорт/удаление никогда не платные»), DPA для облака | SESSION-15 (приёмка) |

### 2.5 Бизнес-слой (только после гейтов)

| Пробел | Что нужно | Откуда |
|---|---|---|
| Нет тарифов | Права Local/Plus/Operator/Developer, локальная офлайн-оценка функций, серверные права — только на сервере | SESSION-15 |
| Нет биллинга | Stripe-подписки, льготный период, поведение при недоступном биллинге, секреты только на сервере | SESSION-15 |
| Нет счётчиков вывода | Счётчики управляемого вывода без исходных промптов; безлимитное тяжёлое рассуждение не рекламируется | SESSION-15 |
| Нет аккаунтов/облака | Зашифрованная синхронизация (Supabase), аутентификация, импорт ChatGPT | P1 (строка 951, §4.3) |

## 3. Дорожная карта по фазам

### Фаза 0 — Валидация (гейт для всего P1)
Blind Preference Test (SESSION-10), удержание, готовность платить, kill criteria (§2.3, строка 191). **Без прохождения — ни биллинга, ни продакшен-запуска** (строка 194). При провале — pivot или остановка, а не «доделывание в стол».

Исполнимый процесс зафиксирован в `docs/validation/P0_VALIDATION_PLAYBOOK.md`, а единственный источник статуса гейта - `docs/validation/P0_VALIDATION_REPORT.md`. Пока отчёт имеет статус `BLOCKED`/`UNDECIDED`, P1 остаётся закрыт. Нельзя заменять внешних участников внутренними тестами или синтетическими данными.

### Фаза 1 — P0 до релизного качества (можно и нужно делать до гейта)
- **SESSION-14**: производительность, токены, надёжность (кеши, отмена запросов, перф-тесты, лимиты контекста).
- **SESSION-16**: полировка, demo-режим, инвесторский сценарий, a11y, безопасные ошибки.
- **SESSION-17**: подписанные сборки, обновления и откат, приватность, диагностика, SBOM, release checklist.

### Фаза 2 — P1 после гейта основателя
- **SESSION-15**: биллинг и доступ к функциям.
- **Настоящий Gateway**: изоляция учётных данных, реальные коннекторы, серверная верификация (§4.11, строка 1111).
- Ключи в OS keychain, корень доверия, облачная синхронизация, аккаунты.

### Фаза 3 — Масштаб
CI/CD на производстве, телеметрия, поддержка пользователей, инцидент-реакция, performance-бюджеты на уровне продукта.

## 4. Чек-лист релиза (база — приёмка SESSION-17)

- [ ] Windows clean install работает; обновление и путь отката остаются незакрытыми
- [ ] Выпуск не содержит секретов или личных тестовых данных
- [ ] Ограничения безопасности видимы пользователю; production-заявление ограничено реализованным поведением P0
- [x] Полные локальные наборы тестов и производительности зелёные на release build; CI-конфигурация добавлена
- [ ] Сборка подписана, SBOM и лицензии проверены
- [ ] Политика приватности опубликована; диагностика маскирует личное; сбои только по согласию
- [ ] Инструкции резервного копирования/экспорта доступны
- [ ] Демонстрационный поток (SESSION-16) проходит без основателя

## 5. Что сознательно не входит

- Универсальная «платформа агентов» — спека требует **узкий** production-выпуск (SESSION-17).
- Безлимитное тяжёлое рассуждение, продажа данных, реклама P0-имитации как защиты production-уровня.
- Мобильные приложения/веб-версия без отдельного решения и валидации.

## 6. Итог

Продакшен — это не одна сессия, а release-hardening + явный гейт валидации + отдельная фаза P1. Ближайшие незакрытые шаги без P1: завершить Tauri/NSIS smoke в стабильном CI, закрыть или принять upstream GTK/glib advisories, добавить SBOM/license audit и получить реальные signing/updater credentials. P0 validation остаётся `BLOCKED` до внешних участников, day-7/day-28 и решения основателя.
